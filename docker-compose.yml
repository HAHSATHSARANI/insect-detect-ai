services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=mongodb+srv://sahandileepa52_db_user:VB26hdtySHLelRwZ@farmer.7y7emts.mongodb.net/?retryWrites=true&w=majority
    volumes:
      - ./backend:/app
      - /app/__pycache__

  goviaruna_admin:
    build: ./goviaruna_admin
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000/api
    depends_on:
      - backend
    volumes:
      - ./goviaruna_admin:/app
      - /app/node_modules

